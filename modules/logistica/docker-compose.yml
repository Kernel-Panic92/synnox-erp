version: '3.8'

services:
  # PostgreSQL para Logistics
  postgres-logistics:
    image: postgis/postgis:15-3.3
    container_name: synnox-logistics-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme_in_docker_env}
      POSTGRES_DB: synnox_logistics
    ports:
      - "5433:5432"
    volumes:
      - logistics-db:/var/lib/postgresql/data
    networks:
      - synnox-network

  # OSRM (Open Source Routing Machine)
  # Imagen precomputada para Colombia
  osrm:
    image: osrm/osrm-backend:v5.27.1
    container_name: synnox-osrm
    volumes:
      # Descarga el mapa de Colombia desde: http://download.geofabrik.de/south-america/colombia-latest.osm.pbf
      # Y colócalo en un volumen o bind mount
      - ./osrm-data:/data
    ports:
      - "5000:5000"
    command: osrm-routed --algorithm mld /data/colombia-latest.osrm
    networks:
      - synnox-network
    # En desarrollo, puedes usar la API pública de OSRM sin levantar este servicio

volumes:
  logistics-db:
    driver: local

networks:
  synnox-network:
    driver: bridge

# Para usar:
# docker-compose up -d
#
# Conexión a PostgreSQL:
# Host: localhost
# Puerto: 5433
# Usuario: postgres
# Password: changeme_in_docker_env
# BD: synnox_logistics
#
# OSRM (local):
# http://localhost:5000
#
# OSRM (en desarrollo - usa el público):
# https://router.project-osrm.org

# SynnoxERP — Nginx configuration
#
# Usar según el modo:
#   test → sudo cp nginx/platform-test.conf /etc/nginx/sites-available/synnoxerp
#   prod → sudo cp nginx/platform-prod.conf /etc/nginx/sites-available/synnoxerp
#
# O ejecutar: sudo bash install.sh test|prod
